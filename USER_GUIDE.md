# 使用手册

> 现金流量表自动编制 Skill · 面向最终用户
> 作者：四川金沐志德科技有限公司 · MIT 许可

本工具依据**资产负债表 + 利润表 + 少量表外数据**，自动编制现金流量表（直接法主表 + 间接法补充资料）。
无需懂编程也能用——下面分两种场景，按你的情况选一种。

---

## 场景一：在 AI 助手里对话使用（推荐·零门槛）

如果你用的是 **Claude Code / WorkBuddy** 等 AI 助手，本 Skill 已安装即可直接对话调用。

### 三步走

**第 1 步：准备数据文件**

把公司的三张表准备好（Excel 或 CSV 均可）：

| 文件 | 内容 | 必需 |
| --- | --- | --- |
| 资产负债表 | 期末数 + 年初数（两列） | ✅ 必需 |
| 利润表（损益表） | 本年累计数 | ✅ 必需 |
| 表外数据收集表 | 工资、税费、折旧等 | ⚪ 可选（但强烈推荐，见下文） |

> 如果三张表在同一个 Excel 工作簿里（三个工作表），只需传一个文件即可。

**第 2 步：把文件发给 AI 助手，说一句话**

直接把文件拖进对话，然后说：

> 帮我编制现金流量表，这是资产负债表和利润表

或者更完整：

> 用现金流量表 skill 帮我编制现金流量表。资产负债表是 [文件]，利润表是 [文件]，
> 表外数据收集表是 [文件]，输出到 output 目录，三种格式都要。

**第 3 步：查看结果**

AI 助手会自动运行引擎，生成三份文件：
- `现金流量表.md`——规范主表 + 补充资料 + 勾稽校验 + 编制附注（人看）
- `现金流量表.xlsx`——Excel 版（给银行/税务/老板）
- `现金流量表.json`——程序处理用

### 对话示例

```
你：帮我编制现金流量表，资产负债表和利润表在"报表2024.xlsx"里
AI：（自动识别三个工作表，运行引擎，输出结果）
你：经营净额为什么偏高？
AI：（解释原因，建议补填表外数据收集表）
```

---

## 场景二：命令行运行（技术人员·可独立部署）

### 环境准备

```bash
# 需要 Python 3.8+
pip install openpyxl xlrd
```

### 最简用法（单工作簿）

如果资产负债表、利润表、表外数据在同一个 Excel 工作簿的三个工作表里：

```bash
python scripts/cash_flow_generator.py --bs 报表.xlsx --out 输出目录 --fmt all
```

### 三张表分开

```bash
python scripts/cash_flow_generator.py \
  --bs 资产负债表.xlsx \
  --pl 利润表.xlsx \
  --extra 表外数据收集表.xlsx \
  --out 输出目录 \
  --fmt all
```

### 支持的输入格式

| 格式 | 说明 |
| --- | --- |
| `.xlsx` | 标准 Excel（推荐） |
| `.xls` | 老版 Excel（用 xlrd 解析） |
| `.csv` | UTF-8 或 GBK 编码均可 |
| Markdown 表格 | 直接粘贴 `| 项目 | 金额 |` 格式文本 |

### 输出格式

`--fmt` 参数可选 `markdown` / `excel` / `json` / `all`（全部）。

---

## 数据怎么准备

### 1. 资产负债表（必需）

**格式要求**：包含"项目名 + 年初数 + 期末数"三列即可。引擎会自动识别科目名。

**科目名兼容**（无需手动转换）：

| 你的报表科目 | 引擎自动识别为 |
| --- | --- |
| 合同负债（新准则） | 预收款项 |
| 应收款项融资（新准则） | 应收票据 |
| 交易性金融资产（新准则） | 短期投资 |
| 递延所得税资产/负债 | 递延税款借/贷项 |
| 使用权资产 / 租赁负债（新租赁准则） | 固定资产 / 长期应付款 |
| 股本 | 实收资本 |
| 营业总收入 / 营业总成本 | 营业收入 / 营业成本 |
| 归母净利润 | 净利润 |
| 开发成本 + 开发产品（房地产） | 存货（自动求和） |
| 拆入资金（金融） | 短期借款 |
| 消耗性生物资产（农业） | 存货 |
| 矿区权益 / 采矿权（能源） | 无形资产 |
| 原材料 + 库存商品 + 在产品（制造业拆分） | 存货（自动求和） |

> 覆盖行业：制造业 / 房地产 / 建筑 / 金融 / 农业 / 能源 / 医药 / 半导体 / 连锁零售 / 军工。
> 完整映射表见 `references/公式口径与编制说明.md`。

**示例**（examples/示例_资产负债表.csv）：

```
项目,年初数,期末数
货币资金,300000,420000
应收票据,150000,120000
应收账款,800000,950000
存货,600000,520000
固定资产,1500000,1800000
...
```

### 2. 利润表（必需）

**格式要求**：包含"项目名 + 本年累计数"即可。

**示例**：

```
项目,本年累计数
营业收入,2000000
营业成本,1200000
销售费用,50000
管理费用,80000
财务费用,20000
净利润,300000
```

### 3. 表外数据收集表（可选·强烈推荐）

> ⭐ **这是控制误差的关键。** 不填→默认模式误差 ±7%~20%；填齐→高精度模式误差 ±0%。

打开 `examples/表外数据收集表.xlsx`，按白色空格填金额即可。分两个区：

**第一区：基础表外数据**（有多少填多少，留空引擎自动推算）

| 项目 | 是否必填 | 说明 |
| --- | --- | --- |
| 支付给职工的工资 | ✅ 必填 | 全年实发工资总额（含奖金津贴） |
| 支付给职工的四金 | 选填 | 社保+公积金（不填按工资×26.6%推算） |
| 销项税额 / 进项税额 | 选填 | 不填按税率推算 |
| 利息支出 | 选填 | 财务费用中的利息部分 |
| 无形资产摊销 | 选填 | 当年摊销额 |
| 偿还债务所支付的现金 | 选填 | 还借款本金 |
| ... | 选填 | 详见收集表说明列 |

**第二区：高精度模式**（要误差归零才填）

从你上期现金流量表主表抄 **6~7 个数**：

| 项目 | 来源 |
| --- | --- |
| 销售商品提供劳务收到的现金 | 现金流量表主表 |
| 购买商品接受劳务支付的现金 | 现金流量表主表 |
| 支付给职工以及为职工支付的现金 | 现金流量表主表 |
| 支付的各项税费 | 现金流量表主表 |
| **收到的其他与经营活动有关的现金** | ⭐ 关键开关！填了=关闭倒挤，误差归零 |
| 支付的其他与经营活动有关的现金 | 现金流量表主表 |
| 收到的税费返还 | 现金流量表主表（有则填） |

> 填了"收到的其他与经营活动有关的现金"后，经营净额 = 各项真实值之和，
> **实测误差 +0.00%**（三一重工/红旗连锁/恩威/海思科/兆易/牧原/益丰/韦尔 8 家验证）。

---

## 输出怎么看

### 现金流量表.md（主输出）

```markdown
# 现金流量表

## 一、经营活动产生的现金流量
  销售商品、提供劳务收到的现金        21,449,673.61
  购买商品、接受劳务支付的现金         8,055,983.63
  ...
  经营活动产生的现金流量净额             89,679.19

## 二、投资活动产生的现金流量
  ...

## 三、筹资活动产生的现金流量
  ...

## 现金及现金等价物净增加额              89,679.19

## 补充资料（间接法）
  净利润                                5,646,774.15
  加：资产减值准备 / 折旧 / 摊销 ...
  经营活动产生的现金流量净额             89,679.19

## 勾稽关系校验
  ✓ 资产负债表勾稽（资产=负债+权益）
  ✓ 利润表勾稽（净利润=未分配利润变动）
  ✓ 现金净增加=货币资金变动
  ✓ 主表经营净额=补充资料经营净额

## 编制附注
  （每项的计算过程说明，便于核对）
```

### 勾稽校验怎么看

| 校验项 | 通过=✓ | 不通过=✗ |
| --- | --- | --- |
| 资产负债表勾稽 | 资产=负债+权益 | 数据录入有误，检查资产负债表 |
| 利润表勾稽 | 净利润=未分配利润变动 | 利润表或分配表数据有误 |
| 现金净增加 | 主表=货币资金变动 | 有理财/定期存款（货币资金≠现金等价物），属正常 |
| 主表=补充资料 | 两个经营净额一致 | 极少出现，检查表外数据 |

---

## 常见问题

### Q1：经营净额偏高/偏低怎么办？

**最常见原因**：表外数据缺失（工资、税费为 0），导致"支付其他"成了倒挤垃圾桶。

**解决**：填表外数据收集表第一区（至少填"支付给职工的工资"），或用高精度模式填主表真实值。

### Q2：有大量理财/定期存款，经营净额偏差大

**原因**：货币资金 ≠ 现金等价物（理财在"交易性金融资产"或"一年内到期非流动资产"里），模板平衡机制被干扰。

**解决**：用**高精度模式**——填收集表第二区 6~7 项主表真实值，误差归零。

### Q3：销售商品收到现金偏高

**常见原因**：应收票据背书转让给供应商（不产生现金），余额变动法无法识别。

**解决**：高精度模式直接填"销售商品提供劳务收到的现金"真实值。

### Q4：税率不是 13%/17% 怎么改？

```bash
python scripts/cash_flow_generator.py --bs 报表.xlsx \
  --json-params '{"sale_vat_rate":0.09, "purchase_vat_rate":0.13}'
```

或在对话里告诉 AI 助手："销项税率 9%、进项税率 13%"。

### Q5：支持哪些行业？

制造业 / 房地产 / 建筑 / 金融（仅非银主营业务）/ 农业 / 能源 / 医药 / 半导体 /
连锁零售 / 军工——科目自动适配，无需手动转换。

**不适用**：银行 / 保险 / 证券（经营现金流结构完全不同，请用监管报表格式）。

### Q6：需要上期金额（对比列）怎么办？

```bash
python scripts/cash_flow_generator.py \
  --bs 本期资产负债表.xlsx --pl 本期利润表.xlsx \
  --bs-prior 上期资产负债表.xlsx --pl-prior 上期利润表.xlsx
```

### Q7：输出能给银行/税务用吗？

可以。`现金流量表.xlsx` 是规范格式（含行次、本期/上期金额、补充资料、附注）。
但请注意：本表反映现金流量**大致基本状况**（编制说明原文），正式申报以企业现金流量表附注为准。

### Q8：能输出英文版吗？

可以。加 `--lang en` 即可输出全英文报表：

```bash
python scripts/cash_flow_generator.py --bs 报表.xlsx --out output --fmt all --lang en
```

或在对话里告诉 AI 助手："用英文输出"。

> 注意：英文模式仅翻译输出层文本（项目名/表头/标题/附注），报表格式仍为中国
> 《企业会计制度》口径，尚未适配 IFRS/US GAAP 的行次结构。

---

## 精度说明

| 模式 | 怎么用 | 误差 | 适用场景 |
| --- | --- | --- | --- |
| 默认模式 | 只传两大主表 | ±7%~±20% | 快速测算、对外报数 |
| 补表外数据 | 填收集表第一区 | ±5% | 日常使用、主表构成可核对 |
| **高精度模式** | 填收集表第二区（6~7 项主表真实值） | **±0%** | 银行授信、税务、重要决策 |

> 14 家上市公司实测验证（茅台/美的/万科/三一/恩威/海思科/兆易/牧原/益丰/沈飞/韦尔/红旗连锁/工行/招行），
> 高精度模式 8 家归零。详见 `references/验证报告-多重测试2026-08.md`。

---

## 还需要帮助？

- **公式怎么算的** → 看 `references/公式口径与编制说明.md`（模板公式逐条对照）
- **精度怎么验证的** → 看 `references/验证报告-多重测试2026-08.md`（14 家上市公司实测）
- **表外数据收集表怎么重新生成** → `python scripts/gen_extra_form.py`
- **原始编制说明** → `references/现金流量表编制说明.txt`

---

*本工具依据《现金流量表编制说明》口径实现，公式与模板完全一致。*
*具体税务处理请与代账会计师或税务师确认后再申报。*
